<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>Nablarch REST Application</display-name>

    <!-- Point DI to rest-boot.xml instead of web-boot.xml -->
    <context-param>
        <param-name>di.config</param-name>
        <param-value>classpath:rest-boot.xml</param-value>
    </context-param>

    <listener>
        <listener-class>nablarch.fw.web.servlet.NablarchServletContextListener</listener-class>
    </listener>

    <!-- Keep the repository-based front controller filter -->
    <filter>
        <filter-name>entryPoint</filter-name>
        <filter-class>nablarch.fw.web.servlet.RepositoryBasedWebFrontController</filter-class>
    </filter>

    <!-- Only REST endpoints -->
    <filter-mapping>
        <filter-name>entryPoint</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>
</web-app>
